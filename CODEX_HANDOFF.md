# Codex 交接说明

这个文件用于把当前对话和项目背景交接给新的 Codex 账号。换账号后，可以直接把本文件内容发给新 Codex，或者让它先读取：

`C:\Users\LFX\Documents\尺寸\size-diagram-generator\CODEX_HANDOFF.md`

## 给新 Codex 的开场提示

请先阅读这个文件，然后继续协助我修改本地项目：

`C:\Users\LFX\Documents\尺寸\size-diagram-generator`

这是一个尺寸图生成器，主要文件是：

`C:\Users\LFX\Documents\尺寸\size-diagram-generator\index.html`

请不要从头重做项目。先理解现有代码和当前改动，再按我的新需求继续小步修改。修改后请检查脚本语法，必要时启动或检查本地页面效果。

## 当前项目状态

- 项目是单页 HTML 应用，核心都在 `index.html`。
- 当前工作区有未提交修改：`index.html` 已被大量调整。
- 最近一次检查方式：从 `index.html` 中提取 `<script>` 内容，然后运行 `node --check`，结果通过。
- 当前 Git 状态曾显示：`M index.html`。
- 不要随意还原文件，不要使用破坏性 Git 命令。

## 用户偏好

- 用户希望界面更简洁，不要控件堆得太多。
- 回答尽量用中文、少技术术语，直接说明改了什么。
- 修改时优先保持现有功能，不要大范围重构。
- 如果涉及 UI，实际表现比代码解释更重要。
- 用户会持续根据截图和效果提出细节调整，需要快速迭代。

## 已完成的重要需求

### 1. 底部虚线和遮挡

- 修过“底部虚线范围”穿过主体盒子的问题。
- 需要让主体盒子能挡住虚线。
- 后面的虚线不能无故中断。

### 2. 文字功能

- 取消右侧“文字内容”输入框。
- 文字直接在画布上双击编辑。
- 编辑时文本出现蓝色高亮的可编辑视觉效果。
- 文本框左上角有删除按钮，点击删除该文字。
- 右侧“删除文字”字段已去掉。
- 字号下拉只保留两个选项：
  - `标准10号字`，值为 `32`
  - `最小9号字`，值为 `28`
- 字段名为：`尺寸标注字号`
- 右侧“添加文字”按钮已加宽并居中。

### 3. 相机和拖拽

- 增加了鼠标中键拖拽调整视图上下左右的功能。
- 所有相机功能已移动到右侧工具栏。
- 右侧工具栏整体加宽，UI 做过重新排布。

### 4. 颜色设置

- 标注线颜色不再可调，固定为 `#FA4E28`。
- 背景立面只保留两个选项：
  - 白色
  - 灰色 `#F5F6FA`
- 墙线改为勾选框：
  - 勾选时颜色固定为 `#A1A1A1`
  - 不勾选时直接隐藏墙线，不是变白
- 轮廓线颜色取消设置，固定为黑色。
- 颜色设置区域不再折叠，直接显示。

### 5. 凹槽颜色和透明度

- 凹槽颜色不再让用户手动调。
- 当背景立面为白色时，凹槽颜色固定为 `#95A1B1`。
- 当背景立面为灰色时，凹槽颜色自动变为 `#65758B`。
- 地面放置模式下，凹槽的后背板、左右侧壁、上下面应使用统一透明度。
- 用户希望凹槽侧面墙壁能透过去看到里面凹槽结构线。

### 6. 商品/桌面颜色

- 商品侧面颜色固定为 `#D2D3D8`，不再可调。
- 桌面放置模式下，商品立面、侧面、桌子凹槽内部、桌子正面也使用 `#D2D3D8`。

### 7. 墙线粗细

- 地面放置模式墙线粗细保持当前效果。
- 桌面放置模式墙线粗细最终要求为 `0.004`。
- 切换地面/桌面模式时，墙线粗细要立即跟着切换，不应只有刷新页面才生效。
- 相关逻辑在 `syncSceneColorMaterials()` 附近，当前有按 `state.layoutMode` 设置线宽。

### 8. 标注线位置

- “标注位置”已改为“标注线位置”。
- 选项文字改为：
  - 自动
  - 样式1
  - 样式2
- 桌面放置模式下也有标注线位置控制，且桌面模式需要考虑三种样式。

### 9. 特殊效果收拢

- 用户希望特殊功能不要散落在界面上。
- 已新增 `特殊效果` 折叠区域。
- 当前放入了：
  - `是否带有凹槽`
  - `间距`
  - `底部虚线范围`
  - 底部虚线相关的范围大小、左右位置、前后位置
- `间距` 是凹槽相关功能，只有勾选 `是否带有凹槽` 后才显示。
- `底部虚线范围` 只在地面放置模式下显示。
- 底部虚线的范围大小/左右位置/前后位置只在启用底部虚线后显示。

## 关键代码位置

以下行号可能会因后续修改变化，只作为定位参考：

- 特殊效果 HTML：`index.html` 中 `specialEffectsPanel`
- 特殊效果样式：搜索 `.special-effects-panel`
- 控件显示隐藏逻辑：`syncControlVisibility()`
- 地面/桌面字段排序：`syncDimensionFieldOrder()`
- 布局切换预设：`applyLayoutPreset(layoutMode)`
- 颜色同步：`syncSceneColorMaterials()`
- 颜色控件同步：`syncColorControls()`
- 文字字号：`textFontSizeSelect`
- 文字叠层：`syncTextOverlays()`
- 中键拖拽和相机控制：搜索 `auxclick`、`button === 1`、`setCameraControl`

## 继续工作时的建议

1. 先运行 `git status --short` 看当前改动。
2. 修改前先读取相关片段，不要凭猜测大改。
3. 改完后检查：
   - 是否有重复的 HTML `id`
   - 控件切换显示是否正确
   - `node --check` 是否通过
4. 如果用户要提交 GitHub：
   - 先确认远程仓库
   - 再提交
   - 最后 push 到用户的 GitHub

## 当前最后一个已完成需求

用户说：“间距也是凹槽的相关功能。”

已处理：

- 把 `间距` 控件移动到了 `特殊效果` 折叠区内。
- 给间距外层加了 `id="gapField"`。
- 在 `syncControlVisibility()` 中加了显示规则：
  - `state.showRecess === "true"` 时显示
  - 否则隐藏
- 已确认 `gapRange` 和 `gapNumber` 没有重复。
- 已通过脚本语法检查。

